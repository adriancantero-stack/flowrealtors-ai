{
    "name": "flowrealtors-monorepo",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd client && npm install && cd ../server && npm install",
        "build": "cd client && npm run build && mkdir -p ../server/public && rm -rf ../server/public/* && cp -r dist/* ../server/public/ && cd ../server && npm run build",
        "start": "cd server && node index.js"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "concurrently": "^8.0.0"
    }
}